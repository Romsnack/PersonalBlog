<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>romsnack</title>
  <subtitle>Notes on AppSec, DevSecOps, and what I learn along the way.</subtitle>
  <id>https://romsnack.github.io/PersonalBlog/</id>
  <updated>2026-08-20T00:00:00Z</updated>
  <link rel="alternate" type="text/html" href="https://romsnack.github.io/PersonalBlog/"></link>
  <link rel="self" type="application/atom+xml" href="https://romsnack.github.io/PersonalBlog/atom.xml"></link>
  <author>
    <name>Romsnack</name>
  </author>
  <entry>
    <title>Writing a static site generator in Go</title>
    <id>https://romsnack.github.io/PersonalBlog/posts/hello-world/</id>
    <updated>2026-08-20T00:00:00Z</updated>
    <published>2026-08-20T00:00:00Z</published>
    <link rel="alternate" type="text/html" href="https://romsnack.github.io/PersonalBlog/posts/hello-world/"></link>
    <summary>Why this blog is 600 lines of Go instead of a Hugo theme, and how the pipeline fits together.</summary>
    <content type="html"><![CDATA[<p>GitHub Pages serves static files and nothing else, so Go can&rsquo;t run when someone
loads a page. That turns out to be freeing rather than limiting: Go runs at
<strong>build time</strong>, and what it produces is plain HTML that any host on earth can
serve.</p>
<h2 id="the-pipeline">The pipeline</h2>
<p>The whole thing runs in one direction, which is the only reason it stays small:</p>
<pre class="chroma"><code><span class="line"><span class="cl"><span class="nx">posts</span><span class="p">,</span><span class="w"> </span><span class="nx">err</span><span class="w"> </span><span class="o">:=</span><span class="w"> </span><span class="nx">content</span><span class="p">.</span><span class="nf">ParseDir</span><span class="p">(</span><span class="s">&#34;content/posts&#34;</span><span class="p">,</span><span class="w"> </span><span class="nx">includeDrafts</span><span class="p">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">if</span><span class="w"> </span><span class="nx">err</span><span class="w"> </span><span class="o">!=</span><span class="w"> </span><span class="kc">nil</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">return</span><span class="w"> </span><span class="nx">err</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nx">site</span><span class="w"> </span><span class="o">:=</span><span class="w"> </span><span class="o">&amp;</span><span class="nx">render</span><span class="p">.</span><span class="nx">Site</span><span class="p">{</span><span class="nx">Config</span><span class="p">:</span><span class="w"> </span><span class="nx">cfg</span><span class="p">,</span><span class="w"> </span><span class="nx">Posts</span><span class="p">:</span><span class="w"> </span><span class="nx">posts</span><span class="p">,</span><span class="w"> </span><span class="nx">Tags</span><span class="p">:</span><span class="w"> </span><span class="nf">indexTags</span><span class="p">(</span><span class="nx">posts</span><span class="p">)}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">return</span><span class="w"> </span><span class="nx">site</span><span class="p">.</span><span class="nf">Build</span><span class="p">(</span><span class="s">&#34;public&#34;</span><span class="p">,</span><span class="w"> </span><span class="s">&#34;static&#34;</span><span class="p">)</span><span class="w">
</span></span></span></code></pre><p>Markdown goes in, <code>[]*Post</code> comes out, the site model indexes it by tag, and
<code>Build</code> writes every page, the feed and the sitemap. There is no cache to
invalidate and no second pass, so a rebuild is always a full rebuild — which
takes a few milliseconds and removes an entire category of bug.</p>
<h2 id="what-each-dependency-buys">What each dependency buys</h2>
<table>
<thead>
<tr>
<th>Module</th>
<th>Job</th>
</tr>
</thead>
<tbody>
<tr>
<td>goldmark</td>
<td>CommonMark-compliant Markdown</td>
</tr>
<tr>
<td>goldmark-frontmatter</td>
<td>the YAML block at the top of this file</td>
</tr>
<tr>
<td>chroma</td>
<td>syntax highlighting, at build time</td>
</tr>
<tr>
<td>fsnotify</td>
<td>file watching for <code>go run . serve</code></td>
</tr>
</tbody>
</table>
<p>Highlighting is worth dwelling on. Chroma is configured with <code>WithClasses(true)</code>,
so the generated HTML carries class names instead of inline styles and the
actual colors live in <code>static/chroma.css</code> next to the rest of the palette. The
consequence: <strong>zero JavaScript ships to readers</strong>. No Prism, no highlight.js, no
flash of unstyled code.</p>
<h2 id="writing">Writing</h2>
<p><code>go run . serve</code> builds the site, serves <code>public/</code> on port 8080, and watches
<code>content/</code> and <code>static/</code>. Save a file and the browser reloads itself over an
<code>EventSource</code> connection. The snippet that does it is one line, and it is gated
behind a dev flag so it never appears in a production build.</p>
<p>That&rsquo;s the whole system. Everything else is CSS.</p>
]]></content>
  </entry>
</feed>